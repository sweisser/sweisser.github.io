# Linux

Blabla

````rust

/// The databases we use within one lmdb environment.
/// (We use them similar to tables in a relational database.)
pub enum LMDBDataBaseType {
    QUOTES,
    SYMBOLS,
    STATS,
    CURRENCIES,
    NAMES,
    DETAILS
}

````
